%h1== Edit Event

- form_for [:admin, @event], :builder => ::WebSgFormBuilder, :html => { :multipart => true } do |f|
  = render :partial => 'form', :object => f
  = f.submit
  or
  = link_to "cancel", admin_events_path

