%h1== Edit Registration Option

- form_for [:admin, @event_option], :builder => ::WebSgFormBuilder do |f|
  = render :partial => 'form', :object => f
  = f.submit
  or
  = link_to "Cancel", admin_events_path

- unless @event_option.new_record?
  %ul
    - for menu_choice in @event_option.menu_choices
      %li
        = menu_choice.description
        = link_to "Edit", edit_admin_menu_choice_path(menu_choice)
        = link_to "Delete", admin_menu_choice_path(menu_choice) , :method => :delete
    %li= link_to "Create new menu option", new_admin_menu_choice_path(:event_option_id => @event_option.id)
