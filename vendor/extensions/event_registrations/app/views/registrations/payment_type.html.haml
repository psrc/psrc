%h2 How would you like to pay?

- form_for :contact, :url => payment_path, :builder => ::WebSgFormBuilder do |f|
  - f.fieldset do
    %p= f.radio_button :payment_type, "Credit Card"
    %p= f.radio_button :payment_type, "Check"

    %p
      = f.submit "Continue"
      or 
      = link_to "back to contact information", contact_info_path(@event, @event_option)
