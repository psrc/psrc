#contact-info
  %h2.title Whom may we contact?
  %p Note: Doesn't have to be the same as attendee information.

  - form_for :contact, :url => payment_type_path(@event,@event_option), :builder => ::WebSgFormBuilder do |f|
    - f.fieldset do
      = f.text_field :first_name, :label => "First Name", :class => "focus"
      = f.text_field :last_name, :label => "Last Name"
      = f.text_field :title, :label => "Title"
      = f.text_field :organization, :label => "Organization"
      = f.text_field :address, :label => "Mailing Address"
      = f.text_field :city, :label => "City"
      = f.text_field :state, :label => "State/Province"
      = f.select :state, ["United States", "Canada"], :label => "Country"
      = f.text_field :zip, :label => "Zip/Postal Code"
      = f.text_field :email, :label => "E-mail Address"
      = f.text_field :phone, :label => "Phone Number"    
    
    %p
      = f.submit "Continue"
      == or #{ link_to "back to attendee information", attendee_info_path(@event, @event_option) }