#contact-info
  %h2.title Whom may we contact?
  %p Note: Doesn't have to be the same as attendee information.

  - form_for :registration_contact, :url => contact_info_path(@event,@event_option), :builder => ::WebSgFormBuilder do |f|
    - f.fieldset do
      = f.text_field :name, :label => "Name", :class => "focus"
      = f.text_field :title, :label => "Title", :hint => "(optional)"
      = f.text_field :organization, :label => "Organization", :hint => "(optional)"
      = f.text_field :address, :label => "Mailing Address"
      = f.text_field :city, :label => "City"
      = f.text_field :state, :label => "State/Province", :hint => "(eg. Washington)"
      = f.select :state, ["United States", "Canada"], :label => "Country"
      = f.text_field :zip, :label => "Zip/Postal Code", :hint => "(eg. 98121)"
      = f.text_field :email, :label => "E-mail Address"
      = f.text_field :phone, :label => "Phone Number", :hint => "(eg. 206-464-7090)"
    
    %p
      = f.submit "Continue"
      == or #{ link_to "back to attendee information", attendee_info_path(@event, @event_option) }
